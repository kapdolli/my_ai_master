# 페이지를 열었을 때 기본으로 체크되어 있을 대학 목록.
#   - 한 줄에 대학 하나. '#' 뒤는 주석.
#   - 대학명은 페이지 목록에 나오는 표기 그대로 (예: 건국대학교(서울))
#   - 비워두면 '전체 대학'(무필터)로 시작한다.
#
# 브라우저에서 체크를 바꾸면 그 브라우저에는 localStorage 로 기억되지만,
# 다른 브라우저/기기까지 옮기려면 페이지의 [선택 내보내기] 로 목록을 복사해
# 이 파일에 붙여넣고 커밋하면 된다 (다음 갱신 때 반영, 최대 10분).
